<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.flowable.idm.engine.impl.persistence.entity.UserEntityImpl">

  <!-- USER RESULTMAP -->
  <resultMap id="userResultMap" type="org.activiti.engine.impl.persistence.entity.UserEntityImpl">
    <id property="id" column="id" jdbcType="VARCHAR" />
    <result property="lastName" column="username" jdbcType="VARCHAR" />
    <result property="email" column="email" jdbcType="VARCHAR" />
    <result property="password" column="password" jdbcType="VARCHAR" />
  </resultMap>

  <!-- USER SELECT -->
  <select id="selectUser" parameterType="string" resultMap="userResultMap">
    select * from ${prefix}pep_auth_users where id = #{id,jdbcType=VARCHAR}
  </select>

  <select id="selectUserByQueryCriteria" parameterType="org.flowable.idm.engine.impl.UserQueryImpl" resultMap="userResultMap">
  	${limitBefore}
    select RES.* ${limitBetween}
    <include refid="selectUserByQueryCriteriaSql" />
    order by RES.username
    ${limitAfter}
  </select>

   <select id="selectUserCountByQueryCriteria" parameterType="org.flowable.idm.engine.impl.UserQueryImpl" resultType="long">
    select count(RES.id)
    <include refid="selectUserByQueryCriteriaSql" />
  </select>

  <sql id="selectUserByQueryCriteriaSql">
    from ${prefix}pep_auth_users RES
    <if test="groupId != null">
      inner join ${prefix}ACT_ID_MEMBERSHIP M on RES.ID_ = M.USER_ID_
      inner join ${prefix}ACT_ID_GROUP G on M.GROUP_ID_ = G.ID_
    </if>
    <where>
      <if test="id != null">
        RES.id = #{id}
      </if>
      <if test="lastName != null">
        and RES.username = #{lastName}
      </if>
      <if test="lastNameLike != null">
        and RES.username like #{lastNameLike}${wildcardEscapeClause}
      </if>
      <if test="fullNameLike != null">
          and RES.username like #{fullNameLike}${wildcardEscapeClause}
      </if>
      <if test="email != null">
        and RES.email = #{email}
      </if>
      <if test="emailLike != null">
        and RES.email like #{emailLike}${wildcardEscapeClause}
      </if>
      <if test="groupId != null">
        and G.ID_ = #{groupId}
      </if>
      <if test="procDefId != null">
        and exists (select ID_ from ${prefix}ACT_RU_IDENTITYLINK where PROC_DEF_ID_ = #{procDefId} and USER_ID_=RES.ID_ )
      </if>
        and RES.valid = 'Y'
        and RES.superuser = 'N'
    </where>
  </sql>

  <select id="selectUserByNativeQuery" parameterType="java.util.Map" resultMap="userResultMap">
    <include refid="org.flowable.engine.db.common.selectByNativeQuery"/>
  </select>

  <select id="selectUserCountByNativeQuery" parameterType="java.util.Map" resultType="long">
    ${sql}
  </select>

</mapper>

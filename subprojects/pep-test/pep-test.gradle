dependencies {
    compile libraries.groovy,
            libraries.junit,
            libraries.spring_test
}

task syncCtx(type: Copy) {
    file('src/main/resources/conf').delete()
    file('src/main/resources/spring').delete()

    copy {
        from project(':pep-webapp').file('src/main/resources/conf')
        into 'src/main/resources/conf'
    }

    copy {
        from project(':pep-webapp').file('src/main/resources/spring')
        into 'src/main/resources/spring'
    }
}

compileJava.dependsOn syncCtx
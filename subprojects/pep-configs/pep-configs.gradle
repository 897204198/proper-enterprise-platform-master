dependencies {
    compile libraries.druid,
            libraries.liquibase,
            libraries.spring_context_support

    compile project(':pep-core-jpa')

    testCompile project(':pep-test')

    testRuntime project(':pep-auth-jwt'),
                project(':pep-auth-common-jpa')
}

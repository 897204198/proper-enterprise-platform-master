<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mongo="http://www.springframework.org/schema/data/mongo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/data/mongo
                           http://www.springframework.org/schema/data/mongo/spring-mongo.xsd"
       default-lazy-init="true">

    <mongo:repositories base-package="com.proper.**.repository" />

    <!-- credentials="${mongodb.username}:${mongodb.password}@admin"  -->
    <mongo:mongo-client id="mongoClient" host="${mongodb.host}" port="${mongodb.port}" />

    <bean id="mongoDatabase" class="com.proper.enterprise.platform.core.utils.MongoDatabaseFactoryBean">
        <property name="mongoClient" ref="mongoClient" />
        <property name="databaseName" value="${mongodb.database}" />
    </bean>

    <bean id="mongoTemplate" class="org.springframework.data.mongodb.core.MongoTemplate">
        <constructor-arg ref="mongoClient"/>
        <constructor-arg name="databaseName" value="${mongodb.database}"/>
    </bean>

</beans>
